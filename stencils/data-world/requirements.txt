psycopg2-binary>=2.9.0
PyYAML>=6.0              # binding.yaml parsing for the scheduler (lib/watch.py, lib/dispatch.py)

# harness.py (optional — only needed if you use the built-in Claude API harness)
anthropic>=0.40.0

# harness_openai.py (optional — only needed if you use the OpenAI API harness)
openai>=1.0.0
